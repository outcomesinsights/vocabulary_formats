
# Dolt database files (added by bd init)
.dolt/
*.db

# Diagnostic logs from `just lint` / `just test` — local artifacts, not repo history
claude_stuff/

# Python bytecode from `just prose` importing the harness next door
__pycache__/
